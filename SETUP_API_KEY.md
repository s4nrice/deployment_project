# 🔑 Настройка API ключа OpenWeatherMap

## Проблема
Вы получили ошибку `401 Unauthorized` при запросе к OpenWeatherMap API. Это означает, что API ключ не настроен или неверный.

## Решение

### Шаг 1: Получите API ключ

1. Перейдите на сайт [OpenWeatherMap](https://openweathermap.org/api)
2. Зарегистрируйтесь или войдите в свой аккаунт
3. Перейдите в раздел [API Keys](https://home.openweathermap.org/api_keys)
4. Создайте новый API ключ или скопируйте существующий

> **Примечание**: Бесплатный план позволяет делать до 60 запросов в минуту и 1,000,000 запросов в месяц.

### Шаг 2: Настройте API ключ в проекте

1. Откройте файл `.env` в корне проекта
2. Найдите строку:
   ```env
   OPENWEATHER_API_KEY=your_api_key_here
   ```
3. Замените `your_api_key_here` на ваш реальный API ключ:
   ```env
   OPENWEATHER_API_KEY=ваш_реальный_api_ключ_здесь
   ```

### Шаг 3: Перезапустите контейнеры

После изменения `.env` файла, перезапустите контейнеры:

```bash
docker-compose down
docker-compose up -d
```

Или перезапустите только контейнер приложения:

```bash
docker-compose restart app
```

### Шаг 4: Проверьте работу

Проверьте, что API ключ работает:

```bash
curl http://localhost:8888/weather/Moscow
```

Если всё настроено правильно, вы получите данные о погоде вместо ошибки 401.

## Пример правильного .env файла

```env
# OpenWeatherMap API
OPENWEATHER_API_KEY=1234567890abcdef1234567890abcdef

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Application
APP_NAME=Weather API
APP_VERSION=1.0.0
DEBUG=False

# Rate Limiting
RATE_LIMIT_REQUESTS=60
RATE_LIMIT_WINDOW=60

# Cache
CACHE_TTL=3600
```

## Важные замечания

- ⚠️ **Не коммитьте файл `.env` в Git** - он уже добавлен в `.gitignore`
- 🔒 **Храните API ключ в секрете** - не публикуйте его в открытых репозиториях
- 📝 **Используйте `env.example`** как шаблон для других разработчиков

## Дополнительная помощь

Если проблема сохраняется:
1. Убедитесь, что API ключ активирован (может потребоваться несколько минут после создания)
2. Проверьте лимиты вашего плана на [OpenWeatherMap](https://home.openweathermap.org/api_keys)
3. Проверьте логи приложения: `docker-compose logs app`

